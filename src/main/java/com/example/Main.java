package com.example;

import java.util.*;

public class Main {
    public static void main(String[] args) {
        // Main method for manual testing
        //Library library = new Library();  // Create a library
        //Librarians librarians = new Librarians();

        // Update: Instantiate and call the new Interface class
        //Interface ui = new Interface();
        Interface.loadInterface();
        //ui.loadInterface(library, librarians);  // Start the new interface with the library and librarians
    }
}
